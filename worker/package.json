{
  "name": "amrs-cloudflare-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "wrangler deploy --dry-run",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}

